# Set this to your deployed backend URL (Render) for production builds.
# Not needed for local dev — Vite proxies /api to http://localhost:3000 (see vite.config.js).
VITE_API_URL=https://your-backend.onrender.com
